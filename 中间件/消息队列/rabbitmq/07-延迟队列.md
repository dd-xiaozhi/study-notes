# 1、概述
## 1.1 什么是延迟队列？
延迟队列就如名字一样，队列中的消息会在指定时间后被消费。

eg：3秒后才能被消费，这个时候队列中虽然是有消息，但是还没到指定时间就不能被消费。就好像你去别人家，主任在拉粑粑，这个时候主人说等我五分钟就出来。一样的道理，延迟队列就是到了指定的时间才能被消费，在此之前消息是不会被消费的。



## 1.2 延迟队列使用场景
1. <font style="color:rgb(0,0,0);">订单在十分钟之内未支付则自动取消 </font>
2. <font style="color:rgb(0,0,0);">新创建的店铺，如果在十天内都没有上传过商品，则自动发送消息提醒。 </font>
3. <font style="color:rgb(0,0,0);">用户注册成功后，如果三天内没有登陆则进行短信提醒。 </font>
4. <font style="color:rgb(0,0,0);">用户发起退款，如果三天内没有得到处理则通知相关运营人员。 </font>
5. <font style="color:rgb(0,0,0);">预定会议后，需要在预定的时间点前十分钟通知各个与会人员参加会议</font>

<font style="color:rgb(0,0,0);">这些场景都有一个特点，需要在某个事件发生之后或者之前的指定时间点完成某一项任务，如： 发生订单生成事件，在十分钟之后检查该订单支付状态，然后将未支付的订单进行关闭；看起来似乎 使用定时任务，一直轮询数据，每秒查一次，取出需要被处理的数据，然后处理不就完事了吗？如果 数据量比较少，确实可以这样做，比如：对于“如果账单一周内未支付则进行自动结算”这样的需求， 如果对于时间不是严格限制，而是宽松意义上的一周，那么每天晚上跑个定时任务检查一下所有未支 付的账单，确实也是一个可行的方案。但对于数据量比较大，并且时效性较强的场景，如：“订单十 分钟内未支付则关闭“，短期内未支付的订单数据可能会有很多，活动期间甚至会达到百万甚至千万 级别，对这么庞大的数据量仍旧使用轮询的方式显然是不可取的，很可能在一秒内无法完成所有订单 的检查，同时会给数据库带来很大压力，无法满足业务要求而且性能低下。
![](https://cdn.nlark.com/yuque/0/2023/png/12747499/1691225169442-2aedf3c7-e37c-4262-ac4f-8d38046d5e1d.png)





# 2、RabbitMQ中的TTL
TTL为消息存活时间，设定消息的存活时间，时间一过还没有被消费就会成为死信。

**两种方式设置消息的TTL**

+ 消息设置TTL方式

在 生产者（消息发送方）设置消息的 TTL
![](https://cdn.nlark.com/yuque/0/2023/png/12747499/1691226115560-2f50fd91-2e6b-4bb1-ac7c-37450dfd9f2f.png)

+ 队列设置TTL方式
    ![](https://cdn.nlark.com/yuque/0/2023/png/12747499/1691227129297-ccf66cef-509e-4af3-8158-64cc830cdad3.png)

**两者的区别**：

队列TTL是整体设置TTL，消息TTL是单个消息设置TTL。消息TTL存在时间到了却还没被消费的情况，这是因为队列它默认是检查第一个消息是否已过期，如果第一个还未过期那么后面过期的消息也是不能被消费的。而队列TTL不会出现这种情况，所以要根据不同的业务使用不同过的方式

:::color1
TiP：

如果同时设置了`消息TTL`和`队列TTL`，那么会选最小TTL的值进行设置

:::





# 3、使用死信队列的方式实现延迟队列
## 3.1 实战说明
消息再超过TTL就会成为死信，通过绑定死信队列可以将死信变成活信，通过这个特性我们可以使用延迟队列。

实现架构图如下：
![](https://cdn.nlark.com/yuque/0/2023/png/12747499/1691398696542-e43f1673-a2cb-4999-a1c8-59bf352968fc.png)

再此架构图中只有一个死信交换机，没有普通交换机的存在，也就是说消息再 TTL 过期之前不会被消费，这也就达到了我们的目的，通过绑定死信交换机来达到延迟队列的效果

**资源列表**

| 队列名称 | 绑定的交换机 | routing key |
| --- | --- | --- |
| QA | X、Y | XA、YD |
| QB | X、Y | XA、YD |
| QD | Y | YD |




## 3.2 队列TTL
### 3.2.1 配置交换机和队列
```java
@Configuration
public class RabbitMQConfig {

    public static final String X_EXCHANGE = "X";
    public static final String Y_DEAD_LETTER_EXCHANGE = "Y";
    public static final String QUEUE_A = "QA";
    public static final String QUEUE_B = "QB";
    public static final String DEAD_LETTER_QUEUE = "QD";
    public static final String QA_X_ROUTING_KEY = "XA";
    public static final String QB_X_ROUTING_KEY = "XB";
    public static final String DEAD_ROUTING_KEY = "YD";


    @Bean("xExchange")
    public DirectExchange xExchange() {
        return new DirectExchange(X_EXCHANGE);
    }

    @Bean("yExchange")
    public DirectExchange yExchange() {
        return new DirectExchange(Y_DEAD_LETTER_EXCHANGE);
    }

    @Bean("queueA")
    public Queue queueA() {
        Map<String, Object> params = new HashMap<>();
        // 声明当前队列绑定死信交换机
        params.put("x-dead-letter-exchange", Y_DEAD_LETTER_EXCHANGE);
        // 声明当前队列的死信路由key
        params.put("x-dead-letter-routing-key", DEAD_ROUTING_KEY);
        // 设置队列的TTL
        params.put("x-message-ttl", 3000);
        return QueueBuilder.durable(QUEUE_A).withArguments(params).build();
    }

    @Bean("queueB")
    public Queue queueB() {
        Map<String, Object> params = new HashMap<>();
        // 声明当前队列绑定死信交换机
        params.put("x-dead-letter-exchange", Y_DEAD_LETTER_EXCHANGE);
        // 声明当前队列的死信路由key
        params.put("x-dead-letter-routing-key", DEAD_ROUTING_KEY);
        // 设置队列的TTL
        params.put("x-message-ttl", 5000);
        return QueueBuilder.durable(QUEUE_B).withArguments(params).build();
    }
    
    // 声明死信队列
    @Bean("queueD")
    public Queue queueD() {
        return new Queue(DEAD_LETTER_QUEUE);
    }

    // 声明队列A绑定X交换机
    @Bean
    public Binding qaBindingX(@Qualifier("queueA") Queue queueA,
                              @Qualifier("xExchange") DirectExchange xExchange) {
        return BindingBuilder.bind(queueA).to(xExchange).with(QA_X_ROUTING_KEY);
    }

    // 声明队列A绑定X交换机
    @Bean
    public Binding qbBindingX(@Qualifier("queueB") Queue queueB,
                              @Qualifier("xExchange") DirectExchange xExchange) {
        return BindingBuilder.bind(queueB).to(xExchange).with(QB_X_ROUTING_KEY);
    }

    // 给死信交换机绑定队列
    @Bean
    public Binding queueBindingD(@Qualifier("queueD") Queue queueD,
                                 @Qualifier("yExchange") DirectExchange yExchange) {
        return BindingBuilder.bind(queueD).to(yExchange).with(DEAD_ROUTING_KEY);
    }
}
```



### 3.2.2 Product
```java
@Slf4j
@RestController
public class SendMsgController {

    @Resource
    private RabbitTemplate rabbitTemplate;

    @GetMapping("/sendMsg/{key}/{msg}")
    public void sendMsg(@PathVariable("key") String key,
                        @PathVariable("msg") String msg) {
        rabbitTemplate.convertAndSend(key, "来自" + key + "的消息：" + msg);
        log.info("{} 发送消息：{}", key, msg);
    }

}
```

根据 `RoutingKey` 发送消息



### 3.2.3 Consumer
```java
@Slf4j
@Component
public class ConsumerTest {

    // 这边这需要消费死信队列中的消息即可
    @RabbitListener(queues = {RabbitMQConfig.DEAD_LETTER_QUEUE })
    public void receiveD(Message message, Channel channel) {
        // 获取第一个接收消息的队列
        String queueName = (String) message.getMessageProperties().getHeaders().get("x-first-death-queue");
        String msg = new String(message.getBody(), StandardCharsets.UTF_8);
        log.info("当前时间为：{}, 接收名为 {} 的消息：{}", new Date(), queueName, msg);
    }

}
```



### 3.2.4 测试
```java
// 发送两个两个请求
curl http://localhost:8080/sendMsg/XA/我是XA
curl http://localhost:8080/sendMsg/XB/我是XB
```


![](https://cdn.nlark.com/yuque/0/2023/png/12747499/1691398880865-002f807b-e66c-4296-abba-4af05485617b.png)

完美~~~





## 3.3、消息TTL
队列TTL可以设置整个队列中所有消息的TTL，但是它的灵活性不够好，对于聊天室预约这种场景就显得有点麻烦，因为每次增加一个不同的TTL就需要新增一个队列，也就是说我们需要重新去配置一个队列，就需要重新编写代码，然后再次上线，这种方式是非常不可取的。

综上所述，我们可以使用消息TTL来定制每条消息的TTL，这样的话就不需要每次有不同的TTL需要增加一个队列。



### 3.3.1 配置添加一个C队列
```java
    public static final String QUEUE_C = "QC";
    
    public static final String QC_X_ROUTING_KEY = "XC";
    	
    @Bean("queueC")
    public Queue queueC() {
        Map<String, Object> params = new HashMap<>();
        // 声明当前队列绑定死信交换机
        params.put("x-dead-letter-exchange", Y_DEAD_LETTER_EXCHANGE);
        // 声明当前队列的死信路由key
        params.put("x-dead-letter-routing-key", DEAD_ROUTING_KEY);
        return QueueBuilder.durable(QUEUE_C).withArguments(params).build();
    }
    @Bean
    public Binding qcBindingX(@Qualifier("queueC") Queue queueC,
                              @Qualifier("xExchange") DirectExchange xExchange) {
        return BindingBuilder.bind(queueC).to(xExchange).with(QC_X_ROUTING_KEY);
    }
```



### 3.3.2 修改消息生产者
这里的话我们设置一个接口参数为消息的TTL

```java
@GetMapping("/sendMsg/{msg}/{ttl}")
public void sendMsg(@PathVariable("msg") String msg,
                    @PathVariable("ttl") Integer ttl) {
    rabbitTemplate.convertAndSend(RabbitMQConfig.X_EXCHANGE, RabbitMQConfig.QC_X_ROUTING_KEY, msg, message -> {
        message.getMessageProperties().setExpiration(String.valueOf(ttl * 1000));
        return message;
    });
    log.info("当前时间为：{}，发送一个 ttl 为 {}秒 的消息：{}",new Date(), ttl, msg);
}
```



### 3.3.3 测试
![](https://cdn.nlark.com/yuque/0/2023/png/12747499/1691400107156-4cef9aea-5ed6-46a4-8f2c-1f64d2d406cb.png)
哈哈，成功~~~😃😃😃

这里我们测试一下前面慢过期的消息和后面快过期消息的情况
![](https://cdn.nlark.com/yuque/0/2023/png/12747499/1691400496150-18e14d6f-32ac-4d3a-9e25-b422c701a0df.png)

可以很清楚的看到它的缺陷。<font style="background-color:#FBDE28;">队列它是默认检查第一个消息是否过期</font>，如果没有那么后面过期的消息也是不能被消费的。下一章我们学习使用插件的方式来避免这种情况





# 4、插件方式实现
RabbitMQ采用插件的方式来弥补消息TTL的缺陷，让消息可以进行细粒度的控制。





## 4.1 安装延时队列插件
1. 下载插件

插件下载地址：[插件下载](https://www.rabbitmq.com/community-plugins.html)，找到 `**<font style="color:rgb(0, 0, 0);">rabbitmq_delayed_message_exchange</font>**`
![](https://cdn.nlark.com/yuque/0/2023/png/12747499/1691478405597-804e4068-0f1e-46cd-a3e8-c1dab55cb5d1.png)

插件下载地址：[https://github.com/rabbitmq/rabbitmq-delayed-message-exchange/releases](https://github.com/rabbitmq/rabbitmq-delayed-message-exchange/releases)

这里我们选择 `3.8.8` 版本的进行下载，根据你们自己的版本下载

2. 安装插件

将下载的插件放到安装 RabbitMQ 根目录下的 `<font style="color:rgb(0,0,0);">plugins</font>`<font style="color:rgb(0,0,0);">目录下</font>

/usr/lib/rabbitmq/lib/rabbitmq_server-3.8.8/plugins	

3. 启用插件

```shell
rabbitmq-plugins enable rabbitmq_delayed_message_exchange
```


![](https://cdn.nlark.com/yuque/0/2023/png/12747499/1691478921963-4da1f200-a0a3-4f2a-8be0-298df49dcd84.png)

4. 查看启动之后的变化


![](https://cdn.nlark.com/yuque/0/2023/png/12747499/1691479079948-2aacc149-0b53-4746-803a-26097b84244e.png)

交换机创建选项中多了一个 `x-delayed-message`选项，我称它为延迟交换机



## 4.2 代码架构图
![](https://cdn.nlark.com/yuque/0/2023/png/12747499/1691483133865-658a1c39-23c3-47ae-baf4-d38ac9f5129c.png)
:::color1
注意的部分：

+ Product：发送一个携带请求头 `x-delay` 的消息
+ 自定义交换机：类型是 direct 、x-delayed-message

:::





## 4.3 实战演练
### 4.3.1 添加延迟对列配置
```java
@Configuration
public class DelayedQueueConfig {

    public static final String DELAYED_QUEUE_NAME = "delayed.queue";
    public static final String DELAYED_EXCHANGE_NAME = "delayed.exchange";
    public static final String DELAYED_ROUTING_KEY = "delayed.queue";

    // 1.创建队列
    @Bean("delayedQueue")
    public Queue queue() {
        return new Queue(DELAYED_QUEUE_NAME);
    }
    // 2.自定义 延迟队列交换机
    @Bean("delayedExchange")
    public CustomExchange delayedExchange() {
        Map<String, Object> args = new HashMap<>();
        // 自定义交换机类型
        args.put("x-delayed-type", "direct");
        return new CustomExchange(DELAYED_EXCHANGE_NAME, "x-delayed-message", true, false, args);
    }

    // 3.进行绑定
    @Bean
    public Binding binding(@Qualifier("delayedQueue") Queue delayedQueue,
                           @Qualifier("delayedExchange") CustomExchange delayedExchange) {
        return BindingBuilder.bind(delayedQueue).to(delayedExchange).with(DELAYED_ROUTING_KEY).noargs();
    }
}
```



### 4.3.1 修改生产者
```java
@Slf4j
@RestController
public class DelayedController {

    @Resource
    private RabbitTemplate rabbitTemplate;

    @GetMapping("delayed/{msg}/{ttl}")
    public void sendMsg(@PathVariable("msg") String msg,
                        @PathVariable("ttl") Integer ttl) {
        rabbitTemplate.convertAndSend(DelayedQueueConfig.DELAYED_EXCHANGE_NAME, DelayedQueueConfig.DELAYED_ROUTING_KEY, msg, message -> {
            // 设置延迟时间
            message.getMessageProperties().setDelay(ttl * 1000);
            return message;
        });
        log.info("当前时间为：{}，发送一个 ttl 为 {}秒 的消息：{}",new Date(), ttl, msg);
    }
}
```

:::color1
Tip：和之前不同的是参数设置

+ 之前：message.getMessageProperties().setExpiration(String.valueOf(ttl * 1000));
+ 现在：message.getMessageProperties().setDelay(ttl * 1000);

他们的区别：

+ `setExpiration`设置的是消息的属性
+ `setDelay` 设置的是请求头 `x-delay`

:::



### 4.3.2 测试
启动访问：[http://localhost:8080/delayed/hello/3](http://localhost:8080/delayed/hello/3)
![](https://cdn.nlark.com/yuque/0/2023/png/12747499/1691482503780-5a58965b-5536-4ad4-99d2-3442d223ec67.png)

我们再来测试一下之前的情况

[http://localhost:8080/delayed/ttl为10/10](http://localhost:8080/delayed/ttl为10/10)

[http://localhost:8080/delayed/ttl为4/4](http://localhost:8080/delayed/ttl为4/4)
![](https://cdn.nlark.com/yuque/0/2023/png/12747499/1691482667940-6fdcd249-c09b-49a8-84a8-05ad58e487b2.png)

结果显示没有出现队列TTL的情况，完美~😁😁😁

